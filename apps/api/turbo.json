{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^db:generate"]
    },
    "dev": {
      "inputs": [".env*"],
      "env": ["API_PORT", "API_URL", "DATABASE_URL"],
      "dependsOn": ["^db:migrate"]
    },
    "test": {
      "inputs": [".env*"],
      "env": ["API_PORT", "API_URL", "DATABASE_URL"],
      "dependsOn": ["^db:migrate:test"]
    }
  }
}
